#: include GUI/layouts/BackButton.kv

<Header@MDBoxLayout>:
    orientation: 'horizontal'
    size_hint_y: 0.1
    pos_hint: {"top": .95}

    padding: dp(15)
    spacing: dp(75)

    BackButton:

    MDRectangleFlatButton:
        size_hint_x: 0.3
        pos_hint: {"center_y": 0.5}
        text: "SKIP"
        on_release: app.root.skip()